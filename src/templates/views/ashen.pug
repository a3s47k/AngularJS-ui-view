.row
	.col-2
		.item(ng-repeat="item in data")
			h2
				a(ui-sref="details({id: item.id})")
					p
						i.fa.fa-fw.mr-2(ng-class="(item.icon.length) ? item.icon : ''")
					p {{item.name}}
	.col-10
